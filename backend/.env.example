PORT=3005
NODE_ENV=development

DB_HOST=your_database_host
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=kittyhp

AUTH_EMAIL=Ramos.Rey@inventec.com
AUTH_PASSWORD=replace-with-initial-admin-password
AUTH_TOKEN_SECRET=replace-with-at-least-32-random-characters
AUTH_TOKEN_LIFETIME_MS=28800000

EMAIL_WORKER_INTERVAL_MS=5000
EMAIL_WORKER_STALE_MINUTES=15

# El backend registra los correos en email_queue con estado pending.
# El worker de Outlook lee esta misma configuración desde backend/.env.
