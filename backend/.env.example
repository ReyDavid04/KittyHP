PORT=3005
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=kittyhp

AUTH_EMAIL=Ramos.Rey@inventec.com
AUTH_PASSWORD=replace-with-initial-admin-password
AUTH_TOKEN_SECRET=replace-with-a-long-random-secret
AUTH_TOKEN_LIFETIME_MS=28800000

# El backend no envía correos directamente.
# Los mensajes se registran en la tabla email_queue con estado pending.
# Utiliza el worker de Outlook de InsighTrack conectado a esta misma base de datos.
